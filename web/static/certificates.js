(()=>{'use strict';const form=document.querySelector('#certificateForm'),status=document.querySelector('#certificateStatus');if(!form)return;const csrf=()=>{const match=document.cookie.match(/(?:^|; )stepanel_csrf=([^;]+)/);return match?decodeURIComponent(match[1]):''};form.addEventListener('submit',async event=>{event.preventDefault();status.textContent='Requesting certificate…';const body=Object.fromEntries(new FormData(form));try{const response=await fetch('/api/certificates/issue',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf()},body:JSON.stringify(body)});const data=await response.json();if(!response.ok)throw new Error(data.error||'Certificate request failed');status.textContent='Certificate issued; verify the Apache site configuration.'}catch(error){status.textContent=error.message}})})();
