(()=>{'use strict';const form=document.querySelector('#cpmoveForm'),status=document.querySelector('#cpmoveStatus'),result=document.querySelector('#cpmoveResult'),label=document.querySelector('#cpmoveFileLabel');if(!form)return;const file=form.querySelector('input[name="backup"]'),user=form.querySelector('input[name="username"]'),database=form.querySelector('input[name="restore_databases"]');const csrf=()=>{const match=document.cookie.match(/(?:^|; )stepanel_csrf=([^;]+)/);return match?decodeURIComponent(match[1]):''};const read=async response=>{const text=await response.text();let data={};try{data=JSON.parse(text)}catch(error){}if(!response.ok)throw new Error(data.error||text||'Request failed');return data};const summary=info=>{const parts=[];parts.push(info.has_home?'website files found':'no website files detected');parts.push((info.databases||[]).length+' database dump(s)');parts.push((info.mailboxes||[]).length+' mailbox(es)');return parts.join(', ')};const wait=async id=>{for(let attempt=0;attempt<720;attempt++){await new Promise(resolve=>setTimeout(resolve,2500));const job=await read(await fetch('/api/jobs/'+encodeURIComponent(id)));if(job.state==='completed'){const value=job.result||{};const dbs=(value.databases_restored||[]).length;const mail=(value.mailboxes_staged||[]).length;result.textContent='Import completed: files '+(value.files_restored?'restored':'not present')+', '+dbs+' database(s), '+mail+' mailbox(es).';return}if(job.state==='failed')throw new Error(job.error||'Import failed')}throw new Error('Import timed out')};file.addEventListener('change',async()=>{result.textContent='';const chosen=file.files&&file.files[0];if(!chosen){label.textContent='Choose .tar.gz backup';status.textContent='Choose a backup to inspect it before restoring.';return}label.textContent=chosen.name;status.textContent='Inspecting backup...';const body=new FormData();body.append('backup',chosen);try{const info=await read(await fetch('/api/cpmove/inspect',{method:'POST',headers:{'X-CSRF-Token':csrf()},body}));if(info.detected_user&&!user.value)user.value=info.detected_user;if((info.databases||[]).length>0)database.checked=true;status.textContent='Ready: '+summary(info)+'.'}catch(error){status.textContent=error.message}});form.addEventListener('submit',async event=>{event.preventDefault();result.textContent='Queueing import...';const body=new FormData(form);try{const data=await read(await fetch('/api/cpmove/import',{method:'POST',headers:{'X-CSRF-Token':csrf()},body}));result.textContent='Import running...';await wait(data.job_id)}catch(error){result.textContent=error.message}})})();
