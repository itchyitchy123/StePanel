(()=>{'use strict';
const target=document.querySelector('#siteOverview'),status=document.querySelector('#siteOverviewStatus'),detail=document.querySelector('#siteDetail');
if(!target||!status||!detail)return;
const csrf=()=>{const match=document.cookie.match(/(?:^|; )stepanel_csrf=([^;]+)/);return match?decodeURIComponent(match[1]):''};
const read=async response=>{const body=await response.text();let data={};try{data=JSON.parse(body)}catch(error){}if(!response.ok)throw new Error(data.error||body||`Request failed (${response.status})`);return data};
const line=(label,value)=>{const item=document.createElement('p'),strong=document.createElement('strong');strong.textContent=label+': ';item.append(strong,document.createTextNode(value||'None yet'));return item};
const setCount=(id,value)=>{const node=document.querySelector(id);if(node)node.textContent=String(value)};
const button=(text,action)=>{const item=document.createElement('button');item.type='button';item.textContent=text;item.addEventListener('click',action);return item};
const createBackup=async(site,output)=>{output.textContent='Creating a verified backup…';try{const data=await read(await fetch('/api/backups',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf()},body:JSON.stringify({site,include_databases:true})}));output.textContent=`Backup queued. Track progress in recent jobs (${data.job_id}).`}catch(error){output.textContent=error.message}};
const connectDomain=(site,output)=>{const form=document.createElement('form'),domain=document.createElement('input'),submit=document.createElement('button');domain.name='domain';domain.required=true;domain.placeholder='example.com';domain.setAttribute('aria-label','Domain to connect');submit.type='submit';submit.textContent='Connect domain';form.append(domain,submit);form.addEventListener('submit',async event=>{event.preventDefault();submit.disabled=true;output.textContent='Checking and connecting domain…';try{await read(await fetch('/api/sites/deploy',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf()},body:JSON.stringify({site,domain:domain.value})}));output.textContent='Domain connected. Point its DNS A/AAAA record to this server to make it live.';await load()}catch(error){output.textContent=error.message}finally{submit.disabled=false}});return form};
const show=async site=>{detail.hidden=false;detail.replaceChildren();const heading=document.createElement('h3');heading.textContent=`${site} workspace`;const loading=document.createElement('p');loading.textContent='Loading site resources…';detail.append(heading,loading);try{const [workspace,databases,history]=await Promise.all([fetch(`/api/sites/overview/${encodeURIComponent(site)}`).then(read),fetch('/api/databases').then(read).catch(()=>({databases:[]})),fetch(`/api/audit/events?target=${encodeURIComponent(site)}&limit=10`).then(read).catch(()=>({events:[]}))]);const output=document.createElement('output');output.setAttribute('role','status');const actions=document.createElement('div');actions.className='site-detail-actions';actions.append(button('Create verified backup',()=>createBackup(site,output)),button('Connect a domain',()=>{actions.replaceWith(connectDomain(site,output))}));detail.replaceChildren(heading,line('Domains',(workspace.routes||[]).map(item=>item.domain).join(', ')),line('Applications',(workspace.applications||[]).map(item=>`${item.domain} (${item.state})`).join(', ')),line('Databases',(databases.databases||[]).filter(item=>item.site===site).map(item=>item.name).join(', ')),line('Recent activity',(history.events||[]).map(item=>item.action).join(', ')),actions,output)}catch(error){loading.textContent=error.message}};
const load=async()=>{const [siteData,backupData]=await Promise.all([fetch('/api/sites/overview').then(read),fetch('/api/backups').then(read).catch(()=>({backups:[]}))]);target.replaceChildren();let domains=0;for(const site of siteData.sites||[]){domains+=(site.routes||[]).length;const card=document.createElement('article');card.className='site-card';const title=document.createElement('h3');title.textContent=site.site;const root=document.createElement('code');root.textContent=(site.routes||[])[0]?.domain||'No domain connected';const summary=document.createElement('p');summary.textContent=`${(site.applications||[]).length} app · ${site.database_count||0} database(s)`;const actions=document.createElement('div');actions.className='site-actions';actions.append(button('Manage site →',()=>show(site.site)));card.append(title,root,summary,actions);target.append(card)}setCount('#siteCount',(siteData.sites||[]).length);setCount('#domainCount',domains);setCount('#backupCount',(backupData.backups||[]).length);status.textContent=siteData.sites&&siteData.sites.length?`${siteData.sites.length} managed site(s). Select one to continue.`:'No managed sites yet. Start by migrating a cPanel backup or deploying a site.'};
load().catch(error=>{status.textContent=error.message});
})();
