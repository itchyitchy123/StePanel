(()=>{'use strict';
const target=document.querySelector('#siteOverview'),status=document.querySelector('#siteOverviewStatus'),detail=document.querySelector('#siteDetail');
if(!target||!status||!detail)return;
const read=async response=>{const body=await response.text();let data={};try{data=JSON.parse(body)}catch(error){}if(!response.ok)throw new Error(data.error||body||`Request failed (${response.status})`);return data};
const line=(label,value)=>{const item=document.createElement('p');const strong=document.createElement('strong');strong.textContent=label+': ';item.append(strong,document.createTextNode(value||'none'));return item};
const show=async site=>{detail.hidden=false;detail.replaceChildren();const heading=document.createElement('h3');heading.textContent=`${site} workspace`;const loading=document.createElement('p');loading.textContent='Loading site resources…';detail.append(heading,loading);try{const [workspace,databases,history]=await Promise.all([fetch(`/api/sites/overview/${encodeURIComponent(site)}`).then(read),fetch('/api/databases').then(read).catch(()=>({databases:[]})),fetch(`/api/audit/events?target=${encodeURIComponent(site)}&limit=10`).then(read).catch(()=>({events:[]}))]);detail.replaceChildren(heading,line('Document root',workspace.document_root),line('Domains',(workspace.routes||[]).map(item=>item.domain).join(', ')),line('Applications',(workspace.applications||[]).map(item=>`${item.domain} (${item.state})`).join(', ')),line('Proxies',(workspace.proxies||[]).map(item=>item.name).join(', ')),line('Databases',(databases.databases||[]).filter(item=>item.site===site).map(item=>`${item.name} (${item.encoding})`).join(', ')),line('Recent activity',(history.events||[]).map(item=>item.action).join(', ')))}catch(error){loading.textContent=error.message}};
const load=async()=>{const data=await read(await fetch('/api/sites/overview'));target.replaceChildren();for(const site of data.sites||[]){const card=document.createElement('article');card.className='site-card';const title=document.createElement('h3');title.textContent=site.site;const root=document.createElement('code');root.textContent=site.document_root;const domains=document.createElement('p');domains.textContent='Domains: '+((site.routes||[]).map(route=>route.domain).join(', ')||'none');const summary=document.createElement('p');summary.textContent=`${(site.applications||[]).length} app · ${site.database_count||0} database(s)`;const button=document.createElement('button');button.type='button';button.className='quiet-action';button.textContent='Manage site →';button.addEventListener('click',()=>show(site.site));card.append(title,root,domains,summary,button);target.append(card)}status.textContent=data.sites&&data.sites.length?`${data.sites.length} managed site(s).`:'No managed sites yet.'};
load().catch(error=>{status.textContent=error.message});
})();
