(()=>{'use strict';const form=document.querySelector('#deployForm'),version=document.querySelector('#nodeVersion'),status=document.querySelector('#deployStatus');if(!form)return;const button=form.querySelector('button[type="submit"]');const csrf=()=>{const match=document.cookie.match(/(?:^|; )stepanel_csrf=([^;]+)/);return match?decodeURIComponent(match[1]):''};const read=async response=>{const text=await response.text();let data={};try{data=JSON.parse(text)}catch(error){}if(!response.ok)throw new Error(data.error||text||`Request failed (${response.status})`);return data};const api=async(path,body)=>read(await fetch(path,{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf()},body:JSON.stringify(body)}));const option=(value,label)=>{const item=document.createElement('option');item.value=value;item.textContent=label;return item};fetch('/api/node/versions').then(read).then(data=>{version.replaceChildren();for(const value of data.versions||[])version.append(option(value,value));if(!version.children.length)version.append(option('','No NVM versions installed'))}).catch(()=>version.replaceChildren(option('','Unable to load versions')));form.addEventListener('submit',async event=>{event.preventDefault();button.disabled=true;status.textContent='Deploying…';const data=Object.fromEntries(new FormData(form));data.port=Number(data.port);try{await api('/api/node/select',{site:data.site,version:data.version});await api('/api/apps/deploy',{site:data.site,domain:data.domain,node_version:data.version,port:data.port});await api('/api/proxy/deploy',{site:data.site,domain:data.domain,backend:data.backend});status.textContent='Application deployed and the selected webserver reloaded.'}catch(error){status.textContent=error.message}finally{button.disabled=false}})})();
