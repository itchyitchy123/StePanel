# Product preview

## Dashboard

![StePanel dashboard preview](assets/dashboard-preview.svg)

This illustration mirrors the dashboard structure using representative values.
At runtime, all service counts, health states, load, security checks, and jobs
come from the current server; the application does not ship simulated activity.

When publishing a release, replace or supplement this illustration with a real screenshot from the tagged build. Real screenshots are more useful to operators evaluating the project.
