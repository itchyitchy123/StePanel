apiVersion: v1
kind: Namespace
metadata:
  name: stepanel
---
apiVersion: v1
kind: Service
metadata:
  name: stepanel
  namespace: stepanel
  labels:
    app.kubernetes.io/name: stepanel
spec:
  selector:
    app.kubernetes.io/name: stepanel
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stepanel-data
  namespace: stepanel
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests: {storage: 10Gi}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stepanel-sites
  namespace: stepanel
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests: {storage: 10Gi}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stepanel
  namespace: stepanel
  labels:
    app.kubernetes.io/name: stepanel
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: stepanel
  template:
    metadata:
      labels:
        app.kubernetes.io/name: stepanel
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: stepanel
          image: ghcr.io/itchyitchy123/stepanel:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: STEPANEL_ENV
              value: "production"
            - name: STEPANEL_LISTEN
              value: ":8080"
            - name: STEPANEL_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: stepanel-secrets
                  key: admin-password
            - name: STEPANEL_SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: stepanel-secrets
                  key: session-secret
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - {name: stepanel-data, mountPath: /var/lib/ste-panel}
            - {name: stepanel-sites, mountPath: /var/www/sites}
      volumes:
        - name: stepanel-data
          persistentVolumeClaim: {claimName: stepanel-data}
        - name: stepanel-sites
          persistentVolumeClaim: {claimName: stepanel-sites}
